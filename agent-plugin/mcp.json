{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "tokenmaxxing": {
      "type": "stdio",
      "command": "./bin/tokenmaxxing-mcp",
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
