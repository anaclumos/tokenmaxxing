{
  "name": "tokenmaxxing",
  "version": "0.1.0",
  "description": "Track and compete on AI coding agent token usage across 14+ platforms",
  "type": "module",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/anaclumos/tokenmaxxing" },
  "keywords": ["claude-code", "codex", "gemini", "token-usage", "leaderboard", "ai-coding"],
  "bin": {
    "tokenmaxxing": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": { "bun": ">=1" },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "dev": "bun run ./src/index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@tokenmaxxing/shared": "workspace:*",
    "citty": "^0.2.1",
    "picocolors": "^1.1.1",
    "zod": "^4.3.6"
  }
}
