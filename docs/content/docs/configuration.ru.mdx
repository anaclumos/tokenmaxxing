---
title: Конфигурация
description: "Схема config.json, пороги и параметры политики"
---

Конфигурация - это JSON в `~/.config/tokenmaxxing/config.json`. Каждое поле необязательно; значения по умолчанию применяются к каждому полю.

```json
{
  "thresholds": { "session": [90], "weekly": 98 },
  "hardThresholds": { "session": 100, "weekly": 100 },
  "claudeBin": "/path/to/real/claude",
  "codexBin": "/path/to/real/codex",
  "policy": {
    "projectionMargin": 0,
    "greedySessionFloor": 80,
    "greedySwapMargin": 0.15,
    "switchModels": ["fable"],
    "usagePollTtlMs": 90000,
    "maxWaitMs": 3600000,
    "checkIntervalMs": 60000
  }
}
```

- `thresholds.session`: пятичасовая лестница, возрастающий список ступеней отсева. Каждая оценка определяет одну активную ступень: самую нижнюю, которую всё ещё проходит хоть один аккаунт пула. Эта ступень служит и триггером, и отсевом кандидатов. Codex читает только верхнюю ступень. См. [Как принимается решение о переключении](/ru/switching).
- `thresholds.weekly`: недельный барьер отсева, покрывающий и 7-дневный агрегат, и каждый недельный лимит по модели.
- `hardThresholds.session` / `hardThresholds.weekly`: барьеры стены (по умолчанию 100, реальный серверный лимит каждого аккаунта). `hardThresholds.session` должен быть не ниже верхней ступени сессии. Когда каждый аккаунт превышает свой барьер отсева, автоматическое решение Claude дожимает каждый из них до его стены перед парковкой - см. [Стена](/ru/switching). Установите его равным верхней ступени и недельному барьеру, чтобы отключить; Codex игнорирует эту настройку.
- `claudeBin` / `codexBin`: закрепляет РЕАЛЬНЫЕ бинарники, которые запускают супервизоры (записываются командами `init` / `init --codex`; диагностика защиты от рекурсии подсказывает исправить здесь `claudeBin`, когда он перестаёт указывать на настоящий бинарник Claude).
- `policy.projectionMargin`: вычитается из каждой ступени сессии, недельного барьера и барьеров стены для формирования действующих порогов.
- `policy.greedySessionFloor`: процент использования сессии, при котором вовлекается жадное схождение.
- `policy.greedySwapMargin`: гистерезис в пользу текущего аккаунта на жадном пути, доля от 0 до 1 (по умолчанию 0.15). Текущий аккаунт удерживает своё место, пока давление темпа припаркованного аккаунта не превысит его больше чем на эту долю, поэтому почти равные аккаунты перестают гонять место туда-сюда и сбрасывать кэш промптов организации. Установите 0, чтобы вернуть прежнее поведение «всегда брать лучший». Это никогда не смягчает пересечённый барьер или стену - только боковые переключения между аккаунтами, которые все находятся под барьерами. Ручная команда `tokenmaxxing switch` игнорирует эту настройку и всегда сходится к лучшему аккаунту.
- `policy.switchModels`: семейства моделей, чей недельный лимит по модели блокирует переключение (записи приводятся к нижнему регистру при загрузке).
- `policy.usagePollTtlMs`: как долго остаётся свежим опрос `/usage` по моделям.
- `policy.maxWaitMs`: граница автоожидания при исчерпанном пуле.
- `policy.checkIntervalMs`: такт периодической проверки (по умолчанию 60000, минимум 10000, потому что launchd по умолчанию не запускает задачу чаще одного раза в 10 секунд). `init` записывает его, округлённым вверх до целых секунд, как интервал таймера (в Linux systemd-параметр `AccuracySec` равен двенадцатой части от него, но не меньше одной секунды), и каждая полоса запаса кратна ему: один такт вблизи барьера и до пяти тактов при большом запасе (см. [Ритм проверок](/ru/switching)). Ожидание при исчерпании и пауза после переключения вместо этого спят до собственных сроков. Расписание читает новое значение при следующей проверке, но установленный таймер сохраняет прежний такт, пока вы снова не выполните `tokenmaxxing init`, а допуск по времени срабатывания равен половине настроенного такта, поэтому изменённый такт полностью учитывается только после этого повторного запуска. С таймером под управлением Nix задайте `checkTimer.intervalSeconds` то же значение; у этой опции тот же нижний предел в 10 секунд.

`tokenmaxxing config` показывает действующую конфигурацию с источниками; `config set`/`unset` редактируют ключи через точку; `config tidy` удаляет неизвестные ключи.
