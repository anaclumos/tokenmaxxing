---
title: Slack 桥接：xx serve
description: "xx serve 通过 Socket Mode 从 Slack 线程运行 Claude Code 会话"
---

`tokenmaxxing serve` 通过 Socket Mode 运行本地 Slack 守护进程，无需公网 URL。设置：

```sh
tokenmaxxing serve setup                    # 打印应用清单，提示输入两个 token
tokenmaxxing serve link <channel-id> <repo> # 把一个频道绑定到一个仓库检出
tokenmaxxing serve                          # 运行守护进程
```

在已链接的频道里提及机器人，它就会为该线程打开 Claude Code 会话，并在已链接的仓库检出中运行。每个新回合都会运行一次带 `resume` 的 Agent SDK `query()`，因此 `ensureBestAccount()` 会在每个回合重新评估账户池，守护进程也可以重启而不丢失线程（线程记录是持久的，守护进程会在启动时重新订阅）；而在某次 query 仍在运行期间到达的回复，会被送入该 query 的流式输入，而不会另外开出自己的 query；错过引导窗口的回复，则会作为下一个回合运行。

需要留意的行为：

- **引导**：一条回复如果是在线程的回合仍在运行时到达，就会被折叠进该回合（这与 Claude Code 在交互模式下使用的回合中途注入方式相同），因此模型会在下一个工具边界看到它并调整方向，而不是先把错的东西做完。回合最后一次工具调用之后才到达的回复，会在其后单独作为一个新回合运行；无论哪种情况都不会丢失消息。被引导的消息会带上沙漏表情回应，并随回合一起收尾，其文本也写入回合的持久标记，因此重启和用量限额恢复都会重放它。只有排在另一条等待消息之后的消息才会跳过折叠（顺序仍会保留），处于排空状态的守护进程仍会显式丢弃消息，而不会去引导回合。
- **流式**：回复原生地流入 Slack，思考和工具运行有任务卡片，围栏代码带高亮渲染，一张收尾的 "Turn" 卡片带成本和时长；一个回合的卡片会折叠成一个可展开的计划区块，因此一个回合通常读起来像一条消息：回复加一个活动下拉菜单（回合中途的重试或被拒绝的续写会另开一条后续消息）。TodoWrite 调用会就地更新同一张清单卡片。Slack 会在服务端让原生流式过期：静默约 30 秒，或者存活满 5 分钟，消息就会被冻结成一枚 "Something went wrong" 标签，因此守护进程会赶在这两个计时器到点之前，干净地收掉闲置或存活过久的消息，再换一条新消息继续。
- **权限**：按链接的 `permissionMode` 默认为 `acceptEdits`；`--yolo`（别名 `--dangerous`）让链接切换到 `bypassPermissions`。中继回合中不允许交互式对话框（没人能通过 Slack 作答），因此模型会以文字提问，你在线程里的回复就是下一个回合。捆绑的插件 skill 会教它在需要决策时提及请求者并结束回合。
- **状态表情回应**：触发回合的那条消息会以 emoji 回应携带其状态：回合运行中是沙漏，随后是对勾（完成）、叉号（失败）或问号（模型需要你的决策）。需要当前清单中的 `reactions:write` 权限；已有应用必须重新粘贴清单并重新安装。
- **关注与提醒**：当模型向你请求决策时，它会调用进程内的 `need_attention` 工具，让守护进程知道该线程正在等你。如果你 10 分钟没有回应，它会在线程里提及你一次。线程处于等待状态时，对提问消息添加表情回应会作为你的回答被中继（竖起大拇指选择推荐选项）；你在被跟踪线程里添加的任何其他表情回应会作为上下文折叠进下一个回合。需要 `reactions:read` 权限和 `reaction_added` 事件。
- **斜杠命令**：一条线程消息在去掉开头提及后若以 `/` 开头，就会作为 claude 斜杠命令运行。Slack 的输入框会吞掉以 `/` 开头的消息，所以请用先提及的形式（`@bot /usage`）或加一个前导空格。
- **用量限额自动恢复**：当整个账户池都触及限额时，短暂的中断会把消息停放下来并就地重试；较长的中断会先发一条等待通知，再在账户池恢复后自动恢复该回合，即使守护进程重启也不受影响。只有恢复时间未知的限额才会要求你重新发送。
- **失败重试**：一个回合尚未完成就失败（claude 子进程崩溃、API 报错）时，会静默重试最多 2 次，并恢复同一个会话，因此已经做完的工作绝不会重跑；只有这些重试都不成，线程才会拿到真正的错误。答案已经完成的回合绝不会重试；而如果这次失败查明是账户池额度耗尽，就交给上面的用量限额机制接手。
- **故障隔离**：一个线程的失败绝不会拖垮其他线程。队列中每个任务各自承担自己的错误（处理器崩溃时，会把一段诊断发到它自己的线程里，守护进程照常服务），并且守护进程还留有一道兜底的 rejection 防线，因为没有它，运行时会为了一个漏网的 rejection 杀掉整个进程，连同每个并发线程手上的回合。
- **优雅关停**：SIGTERM/SIGINT 会触发排空：新回合会被显式丢弃，进行中的回合最多有 5 分钟，claude 子进程则在自己的进程组中分离运行，这样终端的 Ctrl-C 就无法在排空保住该回合之前将其杀掉。
- **线程清理**：当你表示工作已完成时，模型会调用进程内的 `finish_thread` 工具；在回合彻底结束之后，守护进程丢弃线程记录、取消订阅，并发出一条确认消息。重新提及即可开启新会话。
- **错误卫生**：回合出错时只发送裁剪过的纯消息文本，绝不发送原始错误正文。
